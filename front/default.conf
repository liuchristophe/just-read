server {
    listen 80;
    server_name jr-front;
    root /usr/share/nginx/html;
    index index.html index.html;

    location /api {
        proxy_pass http://jr-back:8080/api/scrums;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}